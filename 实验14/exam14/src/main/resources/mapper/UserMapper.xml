<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.sdju.soft.exam14.mapper.UserMapper">

    <!--新增用户-->
    <insert id="addUser" parameterType="cn.edu.sdju.soft.exam14.entity.User" useGeneratedKeys="true" keyProperty="id">
        insert into tb_user (username,password,age,gender,email,create_time,update_time)
        values (#{username},#{password},#{age},#{gender},#{email},#{createTime},#{updateTime})
    </insert>

    <!-- 根据id查找用户 -->
    <select id="getUserById" parameterType="java.lang.Integer" resultType="cn.edu.sdju.soft.exam14.entity.User">
        SELECT id,username,password,age,gender,email,create_time,update_time
        FROM tb_user
        where id = #{id}
    </select>

    <!-- 查找所有用户 -->
    <select id="selectAll" resultType="cn.edu.sdju.soft.exam14.entity.User">
        SELECT id,username,password,age,gender,email,create_time,update_time
        FROM tb_user
    </select>


    <!-- 更新用户信息 -->
    <update id="update" parameterType="cn.edu.sdju.soft.exam14.entity.User">
        UPDATE tb_user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="email != null">email = #{email},</if>
            <if test="age != null">age = #{age},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据id删除用户 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM tb_user
        WHERE id = #{id}
    </delete>

</mapper>